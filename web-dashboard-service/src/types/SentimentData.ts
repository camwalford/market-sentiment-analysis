// Define the type for the data structure
type SentimentData = {
    date: string;
    fullDate: string;
    positive: number;
    negative: number;
    neutral: number;
    neutralLine: number;
}

export default SentimentData;